include /mixins/rating
include /mixins/facility
h4
  span.text-primary.mr-2= location.name
  small
    +rating(location.rating)
  span.badge.float-right.badge-info.text-light= location.distance
p.address= location.address
p
  each facility in location.facilities
    +facility(facility, false)